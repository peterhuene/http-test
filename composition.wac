package component:service;

let middleware = new component:middleware { ... };

let proxy = new component:proxy { outgoing-handler: middleware.outgoing-handler, ... };

export proxy.incoming-handler;
